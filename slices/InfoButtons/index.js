import React from 'react'
import { PrismicRichText } from '@prismicio/react'
import { PrismicLink } from '@prismicio/react'
import Link from 'next/link'

/**
 * @typedef {import("@prismicio/client").Content.InfoButtonsSlice} InfoButtonsSlice
 * @typedef {import("@prismicio/react").SliceComponentProps<InfoButtonsSlice>} InfoButtonsProps
 * @param { InfoButtonsProps }
 */
const InfoButtons = ({ slice }) => (
  <section>
 
  </section>
)

export default InfoButtons